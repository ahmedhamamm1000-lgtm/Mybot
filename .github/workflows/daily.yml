name: Triple Daily Bot
on:
  schedule:
    - cron: '0 7 * * *'  # الساعة 9 صباحاً (توقيت مصر) - 20 رسالة
    - cron: '0 14 * * *' # الساعة 4 عصراً (توقيت مصر) - 10 رسائل
    - cron: '0 22 * * *' # الساعة 12 مساءً (توقيت مصر) - أذكار
  workflow_dispatch:

jobs:
  broadcast:
    runs-on: ubuntu-latest
    steps:
      - name: Send Messages
        run: |
          TOKEN="8547376425:AAGHy5gDX7gaNI9c9r2L4mdvvu7YxIObyEk"
          CHANNEL="@AhmedMods2"
          
          # الحصول على الساعة الحالية
          HOUR=$(date +%H)
          
          if [ "$HOUR" == "07" ]; then
            echo "إرسال ورد الصباح (20 رسالة)..."
            for i in {1..20}; do
              RES=$(curl -s "https://api.aladhan.com/v1/ayah/random/ar.alafasy")
              TXT=$(echo $RES | jq -r '.data.text')
              SUR=$(echo $RES | jq -r '.data.surah.name')
              curl -s -X POST "https://api.telegram.org/bot$TOKEN/sendMessage" \
                -d "chat_id=$CHANNEL" \
                -d "parse_mode=HTML" \
                -d "text=☀️ <b>ورد الصباح ($i/20)</b>%0A%0A$TXT%0A%0A📖 سورة: $SUR"
              sleep 2
            done

          elif [ "$HOUR" == "14" ]; then
            echo "إرسال ورد العصر (10 رسائل)..."
            for i in {1..10}; do
              RES=$(curl -s "https://api.aladhan.com/v1/ayah/random/ar.alafasy")
              TXT=$(echo $RES | jq -r '.data.text')
              curl -s -X POST "https://api.telegram.org/bot$TOKEN/sendMessage" \
                -d "chat_id=$CHANNEL" \
                -d "parse_mode=HTML" \
                -d "text=🌤️ <b>ورد العصر ($i/10)</b>%0A%0A$TXT"
              sleep 2
            done

          else
            echo "إرسال أذكار المساء..."
            ADKAR=("سُبْحَانَ اللَّهِ وَبِحَمْدِهِ" "أَسْتَغْفِرُ اللَّهَ وَأَتُوبُ إِلَيْهِ" "لَا حَوْلَ وَلَا قُوَّةَ إِلَّا بِاللَّهِ" "سُبْحَانَ اللَّهِ الْعَظِيمِ" "اللَّهُمَّ صَلِّ وَسَلِّمْ عَلَى نَبِيِّنَا مُحَمَّدٍ")
            for msg in "${ADKAR[@]}"; do
              curl -s -X POST "https://api.telegram.org/bot$TOKEN/sendMessage" \
                -d "chat_id=$CHANNEL" \
                -d "parse_mode=HTML" \
                -d "text=🌙 <b>أذكار المساء</b>%0A%0A$msg"
              sleep 2
            done
          fi
